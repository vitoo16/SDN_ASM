<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Elite Perfumes</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/navigation.css">
    <link rel="stylesheet" href="/css/admin.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        .form-page-background {
            min-height: 100vh;
            background: radial-gradient(circle at 12% 10%, rgba(212, 186, 255, 0.18), transparent 55%),
                        radial-gradient(circle at 84% 6%, rgba(130, 208, 255, 0.16), transparent 55%),
                        linear-gradient(190deg, #06070c 0%, #111628 58%, #05070c 100%);
            padding: 48px 0;
        }

        .form-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 0 24px;
        }

        .form-card {
            border-radius: 24px;
            border: 1px solid rgba(148, 163, 184, 0.25);
            background: linear-gradient(155deg, rgba(15, 23, 42, 0.94) 0%, rgba(17, 24, 39, 0.9) 100%);
            box-shadow: 0 40px 90px rgba(8, 15, 35, 0.55);
            backdrop-filter: blur(24px);
            overflow: hidden;
        }

        .form-header {
            padding: 24px 32px;
            border-bottom: 1px solid rgba(148, 163, 184, 0.18);
            background: radial-gradient(circle at 18% 15%, rgba(139, 92, 246, 0.22), transparent 60%),
                        radial-gradient(circle at 82% 0%, rgba(59, 130, 246, 0.18), transparent 55%);
        }

        .form-header h1 {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--text-primary);
            margin: 0;
        }

        .form-content {
            padding: 32px;
            background: rgba(15, 23, 42, 0.92);
        }

        .form-description {
            color: rgba(226, 232, 240, 0.68);
            font-size: 0.875rem;
            margin-bottom: 20px;
            font-weight: 500;
        }

        .form-field {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .form-label {
            color: var(--text-secondary);
            font-size: 0.875rem;
            font-weight: 500;
        }

        .form-label .required {
            color: #ef4444;
        }

        .form-input {
            width: 100%;
            padding: 12px 16px;
            border-radius: 12px;
            background: rgba(15, 23, 42, 0.78);
            border: 1px solid rgba(148, 163, 184, 0.35);
            color: var(--text-primary);
            font-size: 1rem;
            font-weight: 600;
            transition: all 0.2s ease;
        }

        .form-input:hover {
            border-color: rgba(193, 156, 255, 0.45);
        }

        .form-input:focus {
            outline: none;
            border-color: rgba(139, 92, 246, 0.9);
            box-shadow: 0 0 0 2px rgba(139, 92, 246, 0.25);
        }

        .form-hint {
            font-size: 0.75rem;
            color: rgba(148, 163, 184, 0.7);
            margin-top: 8px;
        }

        .form-actions {
            padding: 20px 32px;
            border-top: 1px solid rgba(148, 163, 184, 0.18);
            background: rgba(13, 19, 33, 0.92);
            display: flex;
            gap: 12px;
            justify-content: flex-end;
        }

        .btn-cancel {
            padding: 10px 20px;
            border-radius: 12px;
            border: 1px solid rgba(148, 163, 184, 0.35);
            background: rgba(15, 23, 42, 0.6);
            color: rgba(226, 232, 240, 0.78);
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .btn-cancel:hover {
            border-color: rgba(226, 232, 240, 0.55);
            background: rgba(30, 41, 59, 0.8);
        }

        .btn-submit {
            padding: 10px 28px;
            border-radius: 12px;
            border: none;
            background: linear-gradient(135deg, rgba(139, 92, 246, 0.95) 0%, rgba(37, 211, 255, 0.82) 100%);
            color: var(--text-primary);
            font-weight: 700;
            cursor: pointer;
            box-shadow: 0 20px 45px rgba(56, 189, 248, 0.35);
            transition: all 0.2s ease;
        }

        .btn-submit:hover {
            background: linear-gradient(135deg, rgba(139, 92, 246, 1) 0%, rgba(37, 211, 255, 0.95) 100%);
            transform: translateY(-1px);
        }

        .alert {
            padding: 16px;
            border-radius: 12px;
            margin-bottom: 20px;
        }

        .alert-error {
            background: rgba(248, 113, 113, 0.12);
            border: 1px solid rgba(248, 113, 113, 0.45);
            color: rgba(255, 245, 245, 0.92);
        }
    </style>
</head>
<body>
    <%- include('../partials/navigation') %>

    <div class="form-page-background">
        <div class="form-container">
            <div class="form-card">
                <div class="form-header">
                    <h1><%= isEdit ? 'Edit Brand' : 'Add New Brand' %></h1>
                </div>

                <form action="<%= isEdit ? `/admin/brands/edit/${brand._id}` : '/admin/brands/create' %>" method="POST">
                    <div class="form-content">
                        <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
                            <div class="alert alert-error">
                                <%= errorMessage %>
                            </div>
                        <% } %>

                        <p class="form-description">
                            Give this house an expressive name to keep your lineup cohesive.
                        </p>

                        <div class="form-field">
                            <label class="form-label">
                                Brand Name <span class="required">*</span>
                            </label>
                            <input 
                                type="text" 
                                name="brandName" 
                                class="form-input" 
                                value="<%= brand ? brand.brandName : '' %>"
                                required
                                autofocus
                            />
                            <p class="form-hint">
                                Brand names must be unique; existing labels cannot be reused.
                            </p>
                        </div>
                    </div>

                    <div class="form-actions">
                        <button type="button" class="btn-cancel" onclick="window.location.href='/admin/dashboard?tab=brands'">
                            Cancel
                        </button>
                        <button type="submit" class="btn-submit">
                            <%= isEdit ? 'Update' : 'Create' %>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
    <script src="/js/main.js"></script>
</body>
</html>

