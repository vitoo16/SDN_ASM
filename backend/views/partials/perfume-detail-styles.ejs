.perfume-detail-page { min-height: 100vh; padding: 3rem 0; background:
radial-gradient(circle at 10% 0%, rgba(193, 156, 255, 0.08), transparent 55%),
radial-gradient(circle at 90% 0%, rgba(129, 212, 255, 0.12), transparent 45%),
linear-gradient(180deg, rgba(7, 9, 14, 0.9) 0%, rgba(7, 9, 14, 0.95) 60%,
rgba(7, 9, 14, 1) 100%); } .back-btn { display: inline-flex; align-items:
center; gap: 0.5rem; padding: 0.75rem 1.5rem; border-radius: 999px; border: 1px
solid rgba(224, 212, 255, 0.35); background: rgba(255, 255, 255, 0.04); color:
var(--accent-primary); font-weight: 600; text-decoration: none; transition: all
0.3s ease; } .back-btn:hover { background: rgba(224, 212, 255, 0.12);
border-color: rgba(224, 212, 255, 0.55); } .perfume-detail-grid { display: grid;
grid-template-columns: minmax(300px, 500px) 1fr; gap: 3rem; margin-top: 2rem; }
.perfume-image-wrapper { position: sticky; top: 6rem; border-radius: 1.5rem;
overflow: hidden; background: rgba(255, 255, 255, 0.04); border: 1px solid
rgba(255, 255, 255, 0.08); box-shadow: 0 24px 60px rgba(0, 0, 0, 0.45); }
.perfume-detail-image { width: 100%; height: auto; aspect-ratio: 3/4;
object-fit: cover; filter: saturate(115%); } .perfume-info-card { padding:
2.5rem; } .perfume-brand-label { color: var(--accent-primary); font-size:
0.9rem; font-weight: 700; letter-spacing: 0.15em; text-transform: uppercase;
margin-bottom: 0.75rem; opacity: 0.85; } .perfume-detail-title { font-size:
2.5rem; font-weight: 800; color: var(--text-primary); margin-bottom: 1.5rem;
line-height: 1.2; } .perfume-rating-section { display: flex; align-items:
center; gap: 1rem; margin-bottom: 1.5rem; } .rating-stars { display: flex; gap:
0.25rem; } .rating-stars .material-icons { font-size: 1.5rem; } .star-filled {
color: #f6c561; filter: drop-shadow(0 4px 8px rgba(246, 197, 97, 0.4)); }
.star-half { color: #f6c561; } .star-empty { color: rgba(246, 197, 97, 0.3); }
.rating-text { color: var(--text-secondary); font-weight: 600; letter-spacing:
0.03em; } .perfume-price-tag { font-size: 2.5rem; font-weight: 700; background:
linear-gradient(120deg, rgba(208, 189, 255, 1) 0%, rgba(135, 202, 255, 1) 45%,
rgba(255, 247, 220, 1) 100%); -webkit-background-clip: text;
-webkit-text-fill-color: transparent; background-clip: text; margin-bottom:
1.5rem; letter-spacing: 0.02em; } .detail-divider { height: 1px; background:
rgba(255, 255, 255, 0.08); margin: 1.5rem 0; } .perfume-specs { display: grid;
grid-template-columns: repeat(2, 1fr); gap: 1.5rem; margin-bottom: 1.5rem; }
.spec-item { display: flex; align-items: center; gap: 1rem; } .spec-icon {
color: var(--accent-primary); font-size: 1.5rem; filter: drop-shadow(0 4px 8px
rgba(193, 156, 255, 0.4)); } .spec-content { flex: 1; } .spec-label { font-size:
0.8rem; color: var(--text-secondary); margin-bottom: 0.25rem; } .spec-value {
font-size: 1rem; font-weight: 600; color: var(--text-primary); }
.perfume-section { margin-bottom: 1.5rem; } .section-heading { font-size:
1.25rem; font-weight: 700; color: var(--text-primary); margin-bottom: 0.75rem; }
.section-text { color: var(--text-secondary); line-height: 1.8; font-size: 1rem;
} .add-to-cart-btn-large { width: 100%; padding: 1.25rem 2rem; font-size:
1.1rem; margin-top: 1.5rem; } /* Comments Section */ .comments-section {
padding: 2rem; margin-top: 2rem; } .comments-title { font-size: 1.75rem;
font-weight: 700; color: var(--text-primary); margin-bottom: 2rem; }
.comment-form { padding: 1.5rem; background: rgba(255, 255, 255, 0.02); border:
1px solid rgba(255, 255, 255, 0.06); border-radius: 1rem; margin-bottom: 2rem; }
.rating-input { display: flex; gap: 0.5rem; flex-direction: row-reverse;
justify-content: flex-end; } .rating-input input[type="radio"] { display: none;
} .rating-star { cursor: pointer; transition: all 0.2s ease; } .rating-star
.material-icons { font-size: 2rem; color: rgba(246, 197, 97, 0.3); }
.rating-input:hover .rating-star .material-icons { color: #f6c561; }
.rating-input input[type="radio"]:checked ~ .rating-star .material-icons {
color: #f6c561; filter: drop-shadow(0 4px 8px rgba(246, 197, 97, 0.4)); }
.login-prompt { text-align: center; padding: 2rem; background: rgba(255, 255,
255, 0.02); border: 1px solid rgba(255, 255, 255, 0.06); border-radius: 1rem;
margin-bottom: 2rem; } .login-prompt a { color: var(--accent-primary);
text-decoration: underline; font-weight: 600; } .comments-list { display: flex;
flex-direction: column; gap: 1.5rem; } .comment-item { padding: 1.5rem;
background: rgba(255, 255, 255, 0.02); border: 1px solid rgba(255, 255, 255,
0.06); border-radius: 1rem; transition: all 0.3s ease; } .comment-item:hover {
background: rgba(255, 255, 255, 0.04); border-color: rgba(255, 255, 255, 0.12);
} .comment-header { display: flex; justify-content: space-between; align-items:
center; margin-bottom: 1rem; } .comment-author { display: flex; align-items:
center; gap: 1rem; } .author-avatar { width: 3rem; height: 3rem; border-radius:
50%; background: linear-gradient(135deg, #c19cff 0%, #7dd3fc 100%); color:
#0b0d12; display: flex; align-items: center; justify-content: center;
font-weight: 700; font-size: 1.25rem; } .author-name { font-size: 1rem;
font-weight: 600; color: var(--text-primary); } .comment-date { font-size:
0.85rem; color: var(--text-secondary); } .comment-rating { display: flex; gap:
0.25rem; } .comment-rating .material-icons { font-size: 1.25rem; }
.comment-content { color: var(--text-secondary); line-height: 1.7;
margin-bottom: 1rem; } .comment-actions { display: flex; gap: 1rem; padding-top:
1rem; border-top: 1px solid rgba(255, 255, 255, 0.06); } .comment-action-btn {
display: inline-flex; align-items: center; gap: 0.5rem; padding: 0.5rem 1rem;
border-radius: 999px; border: 1px solid rgba(255, 255, 255, 0.12); background:
transparent; color: var(--text-secondary); cursor: pointer; font-size: 0.9rem;
font-weight: 600; transition: all 0.3s ease; } .comment-action-btn:hover {
background: rgba(255, 255, 255, 0.05); } .comment-action-btn .material-icons {
font-size: 1.1rem; } .edit-btn:hover { color: #93c5fd; border-color: rgba(147,
197, 253, 0.35); } .delete-btn:hover { color: #fca5a5; border-color: rgba(252,
165, 165, 0.35); } .empty-state { text-align: center; padding: 3rem 2rem; color:
var(--text-secondary); } .empty-icon { font-size: 4rem; opacity: 0.3;
margin-bottom: 1rem; } /* Modal */ .modal { display: none; position: fixed; top:
0; left: 0; right: 0; bottom: 0; background: rgba(0, 0, 0, 0.8);
backdrop-filter: blur(8px); z-index: 10000; align-items: center;
justify-content: center; padding: 2rem; } .modal-content { background: rgba(12,
15, 21, 0.95); border: 1px solid rgba(255, 255, 255, 0.08); border-radius:
1.5rem; max-width: 600px; width: 100%; padding: 2rem; box-shadow: 0 32px 64px
rgba(0, 0, 0, 0.6); } .modal-header { display: flex; justify-content:
space-between; align-items: center; margin-bottom: 1.5rem; } .modal-header h3 {
font-size: 1.5rem; font-weight: 700; color: var(--text-primary); } .modal-close
{ background: transparent; border: none; color: var(--text-secondary); cursor:
pointer; padding: 0.5rem; border-radius: 50%; transition: all 0.3s ease; }
.modal-close:hover { background: rgba(255, 255, 255, 0.1); color:
var(--text-primary); } .modal-actions { display: flex; gap: 1rem;
justify-content: flex-end; margin-top: 1.5rem; } @media (max-width: 1024px) {
.perfume-detail-grid { grid-template-columns: 1fr; gap: 2rem; }
.perfume-image-wrapper { position: relative; top: 0; } } @media (max-width:
768px) { .perfume-detail-title { font-size: 2rem; } .perfume-specs {
grid-template-columns: 1fr; } .perfume-info-card, .comments-section { padding:
1.5rem; } .modal-content { padding: 1.5rem; } }
