<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Elite Perfumes</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/navigation.css">
    <link rel="stylesheet" href="/css/admin.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        .form-page-background {
            min-height: 100vh;
            background: radial-gradient(circle at 12% 10%, rgba(212, 186, 255, 0.18), transparent 55%),
                        radial-gradient(circle at 84% 6%, rgba(130, 208, 255, 0.16), transparent 55%),
                        linear-gradient(190deg, #06070c 0%, #111628 58%, #05070c 100%);
            padding: 48px 0;
        }

        .form-container {
            max-width: 900px;
            margin: 0 auto;
            padding: 0 24px;
        }

        .form-card {
            border-radius: 24px;
            border: 1px solid rgba(255, 255, 255, 0.12);
            background: rgba(9, 12, 20, 0.94);
            box-shadow: 0 48px 120px rgba(0, 0, 0, 0.65);
            backdrop-filter: blur(30px);
            overflow: hidden;
        }

        .form-header {
            padding: 24px 32px;
            border-bottom: 1px solid var(--divider);
            background: radial-gradient(circle at 15% 20%, rgba(193, 156, 255, 0.14), transparent 55%),
                        radial-gradient(circle at 82% 8%, rgba(126, 205, 255, 0.12), transparent 60%);
        }

        .form-header h1 {
            font-size: 1.6rem;
            font-weight: 700;
            color: var(--text-primary);
            margin: 0;
        }

        .form-content {
            padding: 32px;
            background: rgba(9, 12, 20, 0.6);
        }

        .form-grid {
            display: grid;
            gap: 20px;
            grid-template-columns: repeat(2, 1fr);
        }

        .form-field {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .form-field.full-width {
            grid-column: 1 / -1;
        }

        .form-label {
            color: var(--text-secondary);
            font-size: 0.875rem;
            font-weight: 500;
        }

        .form-label .required {
            color: #ef4444;
        }

        .form-input,
        .form-select,
        .form-textarea {
            width: 100%;
            padding: 12px 16px;
            border-radius: 12px;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.08);
            color: var(--text-primary);
            font-size: 1rem;
            transition: all 0.2s ease;
        }

        .form-input:hover,
        .form-select:hover,
        .form-textarea:hover {
            border-color: rgba(193, 156, 255, 0.5);
        }

        .form-input:focus,
        .form-select:focus,
        .form-textarea:focus {
            outline: none;
            border-color: rgba(193, 156, 255, 0.95);
            box-shadow: 0 0 0 2px rgba(193, 156, 255, 0.25);
        }

        .form-textarea {
            resize: vertical;
            min-height: 80px;
        }

        .form-select option {
            background: #1a1d2e;
            color: var(--text-primary);
        }

        .form-actions {
            padding: 20px 32px;
            border-top: 1px solid var(--divider);
            background: rgba(9, 12, 20, 0.68);
            display: flex;
            gap: 12px;
            justify-content: flex-end;
        }

        .btn-cancel {
            padding: 10px 24px;
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.12);
            background: rgba(255, 255, 255, 0.02);
            color: var(--text-secondary);
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .btn-cancel:hover {
            background: rgba(255, 255, 255, 0.06);
            color: var(--text-primary);
        }

        .btn-submit {
            padding: 10px 24px;
            border-radius: 12px;
            border: none;
            background: linear-gradient(135deg, rgba(139, 92, 246, 0.85) 0%, rgba(45, 212, 191, 0.78) 100%);
            color: var(--text-primary);
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .btn-submit:hover {
            background: linear-gradient(135deg, rgba(139, 92, 246, 0.95) 0%, rgba(45, 212, 191, 0.88) 100%);
            transform: translateY(-1px);
        }

        .alert {
            padding: 16px;
            border-radius: 12px;
            margin-bottom: 20px;
        }

        .alert-error {
            background: rgba(248, 113, 113, 0.12);
            border: 1px solid rgba(248, 113, 113, 0.45);
            color: rgba(255, 245, 245, 0.92);
        }

        @media (max-width: 768px) {
            .form-grid {
                grid-template-columns: 1fr;
            }

            .form-field {
                grid-column: 1 / -1;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/navigation') %>

    <div class="form-page-background">
        <div class="form-container">
            <div class="form-card">
                <div class="form-header">
                    <h1><%= isEdit ? 'Edit Perfume' : 'Add New Perfume' %></h1>
                </div>

                <form action="<%= isEdit ? `/admin/perfumes/edit/${perfume._id}` : '/admin/perfumes/create' %>" method="POST">
                    <div class="form-content">
                        <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
                            <div class="alert alert-error">
                                <%= errorMessage %>
                            </div>
                        <% } %>

                        <div class="form-grid">
                            <div class="form-field">
                                <label class="form-label">
                                    Perfume Name <span class="required">*</span>
                                </label>
                                <input 
                                    type="text" 
                                    name="perfumeName" 
                                    class="form-input" 
                                    value="<%= perfume ? perfume.perfumeName : '' %>"
                                    required
                                />
                            </div>

                            <div class="form-field">
                                <label class="form-label">
                                    Image URL <span class="required">*</span>
                                </label>
                                <input 
                                    type="url" 
                                    name="uri" 
                                    class="form-input" 
                                    value="<%= perfume ? perfume.uri : '' %>"
                                    required
                                />
                            </div>

                            <div class="form-field">
                                <label class="form-label">
                                    Price <span class="required">*</span>
                                </label>
                                <input 
                                    type="number" 
                                    name="price" 
                                    class="form-input" 
                                    value="<%= perfume ? perfume.price : '' %>"
                                    step="0.01"
                                    min="0"
                                    required
                                />
                            </div>

                            <div class="form-field">
                                <label class="form-label">
                                    Volume (ml) <span class="required">*</span>
                                </label>
                                <input 
                                    type="number" 
                                    name="volume" 
                                    class="form-input" 
                                    value="<%= perfume ? perfume.volume : '50' %>"
                                    min="0"
                                    required
                                />
                            </div>

                            <div class="form-field">
                                <label class="form-label">
                                    Concentration <span class="required">*</span>
                                </label>
                                <select name="concentration" class="form-select" required>
                                    <option value="Extrait" <%= perfume && perfume.concentration === 'Extrait' ? 'selected' : '' %>>Extrait</option>
                                    <option value="EDP" <%= !perfume || perfume.concentration === 'EDP' ? 'selected' : '' %>>EDP</option>
                                    <option value="EDT" <%= perfume && perfume.concentration === 'EDT' ? 'selected' : '' %>>EDT</option>
                                    <option value="EDC" <%= perfume && perfume.concentration === 'EDC' ? 'selected' : '' %>>EDC</option>
                                </select>
                            </div>

                            <div class="form-field">
                                <label class="form-label">
                                    Target Audience <span class="required">*</span>
                                </label>
                                <select name="targetAudience" class="form-select" required>
                                    <option value="male" <%= perfume && perfume.targetAudience === 'male' ? 'selected' : '' %>>Male</option>
                                    <option value="female" <%= perfume && perfume.targetAudience === 'female' ? 'selected' : '' %>>Female</option>
                                    <option value="unisex" <%= !perfume || perfume.targetAudience === 'unisex' ? 'selected' : '' %>>Unisex</option>
                                </select>
                            </div>

                            <div class="form-field full-width">
                                <label class="form-label">
                                    Brand <span class="required">*</span>
                                </label>
                                <select name="brand" class="form-select" required>
                                    <option value="">Select a brand</option>
                                    <% brands.forEach(brand => { %>
                                        <option value="<%= brand._id %>" <%= perfume && perfume.brand._id === brand._id ? 'selected' : '' %>>
                                            <%= brand.brandName %>
                                        </option>
                                    <% }) %>
                                </select>
                            </div>

                            <div class="form-field full-width">
                                <label class="form-label">
                                    Description <span class="required">*</span>
                                </label>
                                <textarea 
                                    name="description" 
                                    class="form-textarea" 
                                    rows="3"
                                    required
                                ><%= perfume ? perfume.description : '' %></textarea>
                            </div>

                            <div class="form-field full-width">
                                <label class="form-label">
                                    Ingredients <span class="required">*</span>
                                </label>
                                <textarea 
                                    name="ingredients" 
                                    class="form-textarea" 
                                    rows="2"
                                    required
                                ><%= perfume ? perfume.ingredients : '' %></textarea>
                            </div>
                        </div>
                    </div>

                    <div class="form-actions">
                        <button type="button" class="btn-cancel" onclick="window.location.href='/admin/dashboard?tab=perfumes'">
                            Cancel
                        </button>
                        <button type="submit" class="btn-submit">
                            <%= isEdit ? 'Update' : 'Create' %>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
    <script src="/js/main.js"></script>
</body>
</html>

