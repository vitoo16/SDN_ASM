<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Odour Perfume</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/navigation.css">
    <link rel="stylesheet" href="/css/components.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>
<body>
    <div class="error-page">
        <div class="error-container">
            <div class="error-content glass-panel fade-in-up">
                <!-- Error Icon -->
                <div class="error-icon">
                    <span class="material-icons">error_outline</span>
                </div>
                
                <!-- Error Title -->
                <h1 class="error-title"><%= error %></h1>
                
                <!-- Error Message -->
                <% if (typeof message !== 'undefined' && message) { %>
                    <p class="error-message"><%= message %></p>
                <% } %>

                <!-- Action Buttons -->
                <div class="error-actions">
                    <a href="/" class="btn-primary">
                        <span class="material-icons">home</span>
                        Go Home
                    </a>
                    <button onclick="history.back()" class="btn-secondary">
                        <span class="material-icons">arrow_back</span>
                        Go Back
                    </button>
                </div>
            </div>
        </div>
    </div>
</body>
</html>

<style>
.error-page {
    min-height: 100vh;
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.08) 0%, rgba(147, 51, 234, 0.12) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem 1rem;
}

.error-container {
    max-width: 600px;
    width: 100%;
}

.error-content {
    position: relative;
    overflow: hidden;
    border-radius: 24px;
    padding: 4rem 3rem;
    background: rgba(255, 255, 255, 0.9);
    border: 1px solid rgba(0, 0, 0, 0.1);
    backdrop-filter: blur(14px);
    box-shadow: 0 20px 45px rgba(59, 130, 246, 0.25);
    text-align: center;
}

.error-content::before {
    content: '';
    position: absolute;
    width: 320px;
    height: 320px;
    top: -160px;
    right: -120px;
    background: rgba(59, 130, 246, 0.12);
    border-radius: 50%;
    filter: blur(18px);
}

.error-content::after {
    content: '';
    position: absolute;
    width: 260px;
    height: 260px;
    bottom: -140px;
    left: -100px;
    background: rgba(147, 51, 234, 0.14);
    border-radius: 50%;
    filter: blur(22px);
}

.error-icon {
    position: relative;
    z-index: 1;
    width: 110px;
    height: 110px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(145deg, rgba(239, 68, 68, 0.85) 0%, rgba(220, 38, 38, 0.85) 100%);
    box-shadow: 0 25px 50px rgba(220, 38, 38, 0.25);
    border: 4px solid rgba(255, 255, 255, 0.35);
    margin: 0 auto 2rem;
}

.error-icon .material-icons {
    font-size: 60px;
    color: white;
}

.error-title {
    position: relative;
    z-index: 1;
    font-size: 2.5rem;
    font-weight: 800;
    margin-bottom: 1rem;
    color: #1f2937;
}

.error-message {
    position: relative;
    z-index: 1;
    color: rgba(31, 41, 55, 0.72);
    line-height: 1.8;
    margin-bottom: 2rem;
    max-width: 560px;
    margin-left: auto;
    margin-right: auto;
    font-size: 1.1rem;
}

.error-actions {
    position: relative;
    z-index: 1;
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
}

.btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    background: linear-gradient(135deg, #3b82f6 0%, #9333ea 100%);
    color: white;
    text-decoration: none;
    border-radius: 999px;
    font-weight: 600;
    font-size: 1rem;
    border: none;
    cursor: pointer;
    box-shadow: 0 12px 24px rgba(59, 130, 246, 0.25);
    transition: all 0.3s ease;
}

.btn-primary:hover {
    box-shadow: 0 18px 32px rgba(59, 130, 246, 0.35);
    background: linear-gradient(135deg, #2563eb 0%, #7c3aed 100%);
}

.btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    background: transparent;
    color: #1f2937;
    text-decoration: none;
    border: 2px solid rgba(31, 41, 55, 0.18);
    border-radius: 999px;
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.3s ease;
}

.btn-secondary:hover {
    border-color: #3b82f6;
    color: #3b82f6;
    background: rgba(59, 130, 246, 0.08);
}

@media (max-width: 768px) {
    .error-content {
        padding: 3rem 2rem;
    }

    .error-title {
        font-size: 2rem;
    }

    .error-actions {
        flex-direction: column;
        align-items: center;
    }
}
</style>

